{
    "name": "@arinoto/hello-world.architecture",
    "version": "1.1.0",
    "private": "true",
    "description": "Minimal app using @anthill. Architecture",
    "main": "dist/src/main.js",
    "types": "src/main.ts",
    "scripts": {
        "get": "cdktf get",
        "build": "tsc",
        "clean": "rm -rf dist",
        "compile": "tsc --pretty && npm run post-build",
        "watch": "tsc --watch --pretty && npm run post-build",
        "vis": "vis tsconfig.json > hello.svg",
        "lint": "eslint $npm_config_local_prefix"
    },
    "keywords": [
        "docker",
        "cloud"
    ],
    "author": "dooreelko",
    "license": "LGPL-3.0",
    "engines": {
        "node": ">=14.0"
    },
    "dependencies": {
        "@arinoto/core": "*"
    },
    "devDependencies": {
        "@arinoto/vis": "*",
        "@arinoto/eslint-config": "*"
    }
}