{
    "name": "@anthill/hello-world.architecture",
    "version": "1.0.0",
    "description": "Minimal app using @anthill. Architecture",
    "main": "dist/src/main.js",
    "scripts": {
        "get": "cdktf get",
        "build": "tsc",
        "clean": "rm -rf dist",
        "compile": "tsc --pretty && npm run post-build",
        "watch": "tsc --watch --pretty && npm run post-build",
        "test": "jest",
        "test:watch": "jest --watch",
        "vis": "vis tsconfig.json > hello.svg",
        "lint": "eslint $npm_config_local_prefix"
    },
    "keywords": [
        "docker",
        "cloud"
    ],
    "author": "dooreelko",
    "license": "LGPL-3.0",
    "engines": {
        "node": ">=14.0"
    },
    "dependencies": {
        "@anthill/core": "*"
    },
    "devDependencies": {
        "@anthill/vis": "*",
        "@anthill/eslint-config": "*"
    }
}